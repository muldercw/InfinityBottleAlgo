import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.muldercw.infinitybottle",
  appName: "Infinity Bottle",
  webDir: "out",
  server: {
    androidScheme: "https",
  },
};

export default config;
